// Example Firebase Configuration (copy to config.js and replace values)

const firebaseConfig = {
    apiKey: "YOUR_API_KEY",
    authDomain: "YOUR_PROJECT.firebaseapp.com",
    projectId: "YOUR_PROJECT",
    storageBucket: "YOUR_PROJECT.appspot.com",
    messagingSenderId: "YOUR_MESSAGING_SENDER_ID",
    appId: "YOUR_APP_ID",
    measurementId: "G-MEASUREMENT_ID"
};

// Initialize Firebase
// Uncomment when ready and ensure you have set proper DB rules
// firebase.initializeApp(firebaseConfig);
// const database = firebase.database();

// NOTE: The real `config.js` used in this repo contains the actual
// client configuration for Firebase. Keep `config.js` out of public
// commits if you prefer to manage it via CI or secrets.
